name: mega-login-test2
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: syntax variants
        run: |
          sudo apt-get install -y megatools > /dev/null
          echo "=== variant A: long opts ==="
          megatools ls --username "eni.koyshi.zo+mega001@gmail.com" --password "M3g@V4ult001xK9" 2>&1 | head -2 || true
          echo "=== variant B: short opts ==="
          megatools ls -u "eni.koyshi.zo+mega001@gmail.com" -p "M3g@V4ult001xK9" 2>&1 | head -3 || true